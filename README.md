# Hubspot Dark Image Viewer
Chrome extension for changing backgrounds on img viewer


To install the extension, visit chrome://extensions in Chrome and drag/select the build folder and not the root folder
